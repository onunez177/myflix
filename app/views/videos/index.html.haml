- @genres.each do |genre|
  %header
    %h2= link_to genre.name, genre_path(genre)
  .videos.row
    - genre.videos.each do |video|
      .video.span2
        = link_to image_tag("/tmp/#{video.small_cover_url}.jpg"), video_path(video)